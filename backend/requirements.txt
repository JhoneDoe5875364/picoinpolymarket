fastapi==0.115.14
pydantic==2.11.7
starlette==0.40.0
httpx
# Pin uvicorn: 0.51 removed uvicorn.workers, which breaks
# `gunicorn -k uvicorn.UvicornWorker`. Run uvicorn directly, or install
# the separate `uvicorn-worker` package and use uvicorn_worker.UvicornWorker.
uvicorn==0.49.0
python-dotenv
asyncpg
certifi
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
PyJWT
requests
pytz
